{
  "name": "pi-tin",
  "version": "0.4.0",
  "author": "dave-tn",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dave-tn/pi-tin.git"
  },
  "devDependencies": {
    "@inquirer/prompts": "^8.5.2",
    "@types/bun": "^1.3.14",
    "@types/node": "^25.9.4",
    "chalk": "^5.6.2",
    "commander": "^14.0.3",
    "typescript": "^6.0.3",
    "valibot": "^1.4.2",
    "yaml": "^2.9.0"
  },
  "bin": {
    "pi-tin": "dist/cli.js",
    "pt": "dist/cli.js"
  },
  "bugs": {
    "url": "https://github.com/dave-tn/pi-tin/issues"
  },
  "cpu": [
    "arm64"
  ],
  "description": "Let AI coding agents run free (permissions bypassed) — in lightweight, VM-isolated sandboxes on macOS, via Apple's container CLI",
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist/"
  ],
  "homepage": "https://github.com/dave-tn/pi-tin#readme",
  "keywords": [
    "agentic",
    "agents",
    "ai-agents",
    "ai-coding-agent",
    "apple-container",
    "apple-silicon",
    "claude-code",
    "cli",
    "containers",
    "developer-tools",
    "local",
    "macos",
    "microvm",
    "sandbox",
    "virtualization",
    "vm"
  ],
  "license": "GPL-3.0-or-later",
  "os": [
    "darwin"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "bun build src/cli.ts --outdir dist --target node --define \"PKG_VERSION='$(node -p \"require('./package.json').version\")'\" --define \"PKG_HOMEPAGE='$(node -p \"require('./package.json').homepage.split('#')[0]\")'\"",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun test && bun run build",
    "prepublishOnly": "bun run check"
  },
  "type": "module"
}
